/**
 * Aurus Storefront — shared section types.
 *
 * Re-exports the platform-level types and adds any Aurus-specific
 * shared interfaces used across multiple section components.
 */

export type {
  SectionComponentProps,
  ResolverContext,
  ValidationResult,
  Viewport,
} from '@/themes/registry/types';
